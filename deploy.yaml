name: agents
type: docker
subdomain: agents          # -> agents.jrdevelopr.site
port: 8088                 # host port (0.0.0.0) -> http://192.168.20.108:8088
container_port: 80
public: true
auth: false                # OPEN on purpose — shareable comparison page, NO lab-gate
self_managed: true         # tiny caddy:2-alpine file-server; brought up manually, not deploy.sh
icon: scale-balance
# Static one-pager: "Agent Arena" — comparison of self-hosted (and notable paid) AI agent
# platforms. Data lives in site/index.html (a JS array — add a platform = add one object).
